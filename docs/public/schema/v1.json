{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "defaultLocale": {
      "type": "string",
      "minLength": 1
    },
    "locales": {
      "minItems": 1,
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "localeDir": {
      "type": "string",
      "minLength": 1
    },
    "provider": {
      "type": "string",
      "minLength": 1,
      "default": "openai",
      "description": "Provider ID (e.g. 'openai', 'anthropic') or custom provider ID"
    },
    "model": {
      "type": "string",
      "description": "Model name passed to the provider (e.g. gpt-4o-mini)"
    },
    "apiKey": {
      "type": "string",
      "minLength": 1
    },
    "baseURL": {
      "type": "string",
      "format": "uri"
    },
    "glossary": {
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {
        "type": "string"
      }
    },
    "maxRetries": {
      "type": "integer",
      "minimum": 0,
      "maximum": 10
    },
    "processor": {
      "type": "string",
      "enum": ["passthrough", "icu"]
    },
    "modelParams": {
      "type": "object",
      "description": "Additional parameters passed to the provider (e.g. max_tokens, extra_body)"
    }
  },
  "required": ["defaultLocale", "locales", "localeDir", "provider", "apiKey"],
  "additionalProperties": false,
  "$id": "https://www.schemastore.org/intl-ai.json",
  "title": "intl-ai config",
  "description": "Configuration for the intl-ai translation build step"
}
